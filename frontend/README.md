
  # React app with pages

  This is a code bundle for React app with pages. The original project is available at https://www.figma.com/design/PPkhHs2ZzeDw2oqhQSrUeq/React-app-with-pages.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  